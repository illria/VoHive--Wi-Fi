# VoHive UI system

VoHive 的前端采用偏 Vercel 的控制台视觉：中性黑白灰、清晰边框、克制阴影和紧凑的信息层级。UI 调整只影响展示层，不能改变现有路由、API 请求、轮询或设备操作行为。

## 视觉规则

- 页面背景使用 `--vh-bg`，内容卡片使用 `--vh-surface`，边界优先使用 1px `--vh-border`。
- 主要操作使用黑色（暗色主题使用白色），链接和焦点状态使用蓝色；状态不能只依赖颜色，应同时提供文字或图标。
- 常用圆角为 8px、10px、12px；避免大面积渐变、玻璃模糊和装饰性背景。
- 页面标题、导航和表单使用系统无衬线字体；IP、时间、设备 ID 等值使用等宽字体。
- 间距优先使用 8px 的倍数；移动端内容边距不小于 16px，交互目标不小于 44px。

## 交互规则

- 所有可操作元素都必须支持键盘访问，并保留可见的 `:focus-visible` 焦点环。
- 加载、空状态、错误和成功状态都要给出明确反馈和下一步动作。
- 页面在移动端、桌面端和宽屏下都保持可读，不依赖横向滚动；动画遵守 `prefers-reduced-motion`。
- 新页面应复用 `PageHeader`、`ui-panel`、`ui-card` 和 `--vh-*` 设计令牌，避免重新引入局部颜色和阴影体系。

参考：[Vercel Web Interface Guidelines](https://vercel.com/design/guidelines)
